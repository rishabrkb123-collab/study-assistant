# Core UI
streamlit>=1.30.0

# LLM via Ollama
ollama>=0.1.8

# Embeddings
# torch installed separately in install-dependencies.bat (CPU wheel)
sentence-transformers>=2.6.0
transformers>=4.38.0
huggingface-hub>=0.21.0

# Vector DB  (0.5+ ships pre-built wheels for Python 3.12 on Windows)
chromadb>=0.5.0

# LangChain (text splitting only)
langchain>=0.1.0
langchain-community>=0.0.29
langchain-text-splitters>=0.0.1

# Document loaders
pymupdf>=1.23.0

# Utilities
numpy>=1.24.0
